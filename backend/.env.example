# Backend environment example for MailFortress
# Copy to .env and fill with real values. Do NOT commit secrets to version control.

# Supabase admin (service role) values - keep these secret and only use on backend
SUPABASE_URL=https://<your-project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sb_secret_xxx

# Generative AI (Gemini) API key - keep secret
GENAI_API_KEY=ya29.your_service_account_or_api_key

# Server port
PORT=3001
